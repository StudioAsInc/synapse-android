<?xml version="1.0" encoding="UTF-8"?><shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <!-- Background fill -->
    <solid android:color="@color/drawable_surface" />
    <!-- Border stroke -->
    <stroke android:width="1dp" android:color="@color/drawable_outline"/>
    <!-- Rounded corners -->
    <corners android:radius="300dp"/>
</shape>
